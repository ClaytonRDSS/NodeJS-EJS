<!DOCTYPE html>
<html lang="pt-br">

    <!--Comando EJS para incluie arquivos esterno na pagina -->
    <%- include('../partials/head'); %>

    <body class="container">
        <%- include('../partials/header'); %>
        <main>
            <div class="jumbotron">
                <h1>Node Com EJS</h1>
                <p>Vamos aprender a trabalhar com arquivos EJS</p>
                <p>  <%= subtitle %> </p>
            </div>

            <ul>
                <!-- forEach fica observando olha elemento por elemento e roda o objeto do cerver e renderiza na pagina-->
                <% qualitys.forEach(function(quality) { %>

                    <li><strong><%= quality.title %></strong><%= quality.message %></li><!-- Mostrar o conteudo do array de objetos dinamicamente.-->

                <% }) %>

            </ul>
        </main>

      

        <%- include('../partials/footer'); %>
    </body>
</html>